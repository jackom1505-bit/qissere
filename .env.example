# Server-only: paste your Neon connection string here (keep sslmode=require).
DATABASE_URL="postgresql://REPLACE_ME:REPLACE_ME@ep-REPLACE_ME.example.invalid/neondb?sslmode=require"

# Admin dashboard at /admin. Replace both placeholders before use.
# At least 16 characters. Choose a unique password.
ADMIN_PASSWORD="REPLACE_ME_WITH_A_STRONG_ADMIN_PASSWORD"
# Generate with: openssl rand -hex 32
ADMIN_SESSION_SECRET="REPLACE_ME_WITH_A_RANDOM_SESSION_SECRET"
